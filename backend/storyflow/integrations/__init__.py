"""Phase 8 real-provider adapters. Each sits behind an existing abstraction (SubtitleClient / AgentRunner /
RunnerProvider) and is only constructed when selected in ``storyflow.providers.ProviderConfig``."""
